# 1. Create the virtual environment using python3
python3 -m venv venv

# 2. Activate it (This is the Mac/Linux command)
source venv/bin/activate

# 3. Now install the tools (it won't ask for permission now!)
pip install streamlit pandas groq python-dotenv
